export enum Gender {
  male = 'm',
  female = 'f',
  undefined = 'n'
}

export enum TicketType {
  abierto_sencillo = 1,
  sencillo = 2,
  redondo = 3,
  abierto_redondo = 4
}

export enum TicketTypeRoute {
  ida = 1,
  regreso = 2
}

export enum Origin {
  sucursal = 1,
  web = 2,
  kiosko = 3,
  app_cliente = 4,
  app_chofer = 5
}
